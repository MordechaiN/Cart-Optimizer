# Cart Optimizer configuration (v0).
#
# Copy to `.env` and adjust if needed. v0 is stateless and has no secrets,
# so this only controls where the server listens.

# Host/port the API + Web UI bind to inside the container.
APP_HOST=0.0.0.0
APP_PORT=8000
